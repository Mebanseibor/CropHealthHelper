<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project"
        tools:targetApi="31">

        <service android:name=".testmedia.MusicService"/>
        <service android:name=".testtwo.DownloadService"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".gallery.GalleryActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".network.NetworkActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".test.TestActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".testmedia.MediaPlayerActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".testtwo.TestTwoActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".helplines.HelplinesActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".notes.NotesActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".notes.CreateNotesActivity"
            android:exported="false">
        </activity>

    </application>

</manifest>